package gridss;

/**
 * Hello world entry point useful for testing whether the correct version of java is installed.
 */
public class Echo {
	public static void main(String[] argv) {
		for (String s : argv) {
			System.out.println(s);
		}
        System.exit(0);
    }
}
